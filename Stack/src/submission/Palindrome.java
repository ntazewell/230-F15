package submission;
import LinkedStack

public class Palindrome {
	public static boolean isPalindrome(String s) {
		// TODO use a stack to determine if s is palindrome
		String T = new array[];
		
		//not sure how to break string into array, then push each 
		//character individually
			

		return false;
	}
}
